<Window x:Class="WpfApplication1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApplication1"
        mc:Ignorable="d"
        Title="MainWindow" Height="675.119" Width="731.775">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <ToolBar Grid.Row="0" Grid.Column="0">
            <Button x:Name="NextButton" Click="NextButton_Click">Next &gt;</Button>
        </ToolBar>
        <Canvas x:Name="DiscardPileCanvas" Grid.Column="0" Grid.Row="1" Height="{Binding Path=UIParameterManager.CardHeight}" />
        <Canvas x:Name="BeanstalkStackCanvas" Grid.Column="1" Grid.Row="1" Height="{Binding Path=UIParameterManager.CardHeight}"/>
        <Canvas x:Name="CastleStackCanvas" Grid.ColumnSpan="2" Grid.Row="2"/>
    </Grid>
</Window>
